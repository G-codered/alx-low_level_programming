#include <stdio.h>
/**
 * main - print string in putchar function
 *
 * Descriiption: using main function
 * this program prints numerals
 * Return: (0)
 */

int main(void)

{
	char c;
	if (c = '0'; c <= '9'; c++)
	putchar(c);

	if (c = 'a'; c <= 'f'; c++)
	putchar(c);

	putchar('\n');
	return (0);
}

