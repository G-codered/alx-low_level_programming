#include <stdio.h>

/**
 * main - print the string in putchar function
 * 
 * Description: using the main function
 * this program prints lowercase alphabets in reverse
 * Return: (0)
 */

int main(void)
	
{
	char c;

	if (c = 'z' ; c >= 'a' ; c++)
	putchar(c);

	putchar('\n');
	return (0);
}
